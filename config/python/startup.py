import math
from math import *

def lg(exp):
    return log(exp, 10)
def ln(exp):
    return log(exp, e)

def quad(a, b, c):
    return [(-b + sqrt(b**2 - 4*a*c)) / (2*a), (-b - sqrt(b**2 - 4*a*c)) / (2*a)]

g=9.8
